import { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import { api, type Competitor, type Product } from '../../shared/api/client';
import { Button, Card, PageHeader } from '../../shared/components/ui';
export function DashboardPage() { const [products,setProducts]=useState<Product[]>([]); const [competitors,setCompetitors]=useState<Competitor[]>([]); useEffect(()=>{void api.listProducts().then(setProducts); void api.listCompetitors().then(setCompetitors);},[]); const active=products.filter(p=>p.status==='active').length; return <><PageHeader title="Dashboard" description="Daily command center for decisions that increase Allegro store profit." action={<div className="actions"><Link to="/products/new"><Button>Add product</Button></Link><Link to="/competitors/new"><Button className="secondary">Add competitor</Button></Link></div>}/><div className="stats"><Card><span>Total products</span><strong>{products.length}</strong></Card><Card><span>Active offers</span><strong>{active}</strong></Card><Card><span>Competitors</span><strong>{competitors.length}</strong></Card><Card><span>Tasks</span><strong>0</strong></Card></div><div className="grid two"><Card><h2>Today Focus</h2><p>Start by adding core products and competitors. v0.1 keeps the workflow simple while future AI recommendations are prepared.</p><div className="actions"><Link to="/products"><Button>Open products</Button></Link><Link to="/competitors"><Button className="secondary">Open competitors</Button></Link></div></Card><Card><h2>AI Business Consultant</h2><p className="muted">Coming next: pricing checks, profit warnings, SEO improvements and purchasing suggestions.</p></Card></div></>; }
