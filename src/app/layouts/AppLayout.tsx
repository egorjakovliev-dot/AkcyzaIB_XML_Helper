import { NavLink, Outlet, useLocation } from 'react-router-dom';
import { navigation } from '../../shared/constants/navigation';
export function AppLayout() { const location = useLocation(); const active = navigation.find(n => n.path === location.pathname) ?? navigation.find(n => location.pathname.startsWith(n.path) && n.path !== '/'); return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="logo">AI</div><div><strong>AkcyzaIB</strong><span>Allegro Consultant</span></div></div><nav>{navigation.map(item => <NavLink key={item.path} to={item.path} end={item.path === '/'}><item.icon size={18}/><span>{item.label}</span></NavLink>)}</nav></aside><main><header className="topbar"><span>Interactive Prototype v0.1</span><strong>{active?.label ?? 'Workspace'}</strong></header><div className="content"><Outlet /></div></main></div>; }
